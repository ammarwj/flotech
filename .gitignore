# ---- OS ----
.DS_Store
Thumbs.db

# ---- Env & secrets ----
.env
.env.*
!.env.example
*.pem
api/storage/oauth-*.key
api/storage/jwt/*.pem

# ---- Node / Next.js (web) ----
web/node_modules/
web/.next/
web/out/
web/build/
web/.turbo/
web/next-env.d.ts
web/.vercel/
web/coverage/

# ---- PHP / Laravel (api) ----
api/vendor/
api/node_modules/
api/public/build/
api/public/hot
api/storage/*.key
api/storage/framework/cache/data/*
api/storage/logs/*.log
api/bootstrap/cache/*.php
api/.phpunit.cache/
api/.phpunit.result.cache

# ---- Docker ----
docker/data/
certbot/

# ---- Custom domain runtime state ----
# Only appears if the repo happens to be checked out at /opt/flo-event, the
# same path config/domains.php's default paths point at (see deploy/setup-
# custom-domains.sh: ROOT=/opt/flo-event). Written by the containers/setup
# script, not by us — never commit it.
/flags/
/letsencrypt/
/certbot-www/
/nginx/custom-domains.conf

# ---- Backup DB (db-backup.sh / db-restore-local.sh) ----
backups-data-prod/

# ---- Editor ----
.idea/
.vscode/
*.swp


CLAUDE.md
sql